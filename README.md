# msc

To run the LLM chatbot, you need to run the respective file inside the Recommender folder, along with a file that contains OpenAI's API credentials. You insert the dummy patient ID, answer the rpompted questions, and get a recommendation for physical activity. 
